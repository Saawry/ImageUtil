<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SampleImageUtil" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.SampleImageUtil" parent="Base.Theme.SampleImageUtil" />
    <!-- Dedicated theme for CropImageActivity to ensure ActionBar is present for menus -->
    <style name="Theme.CropImage.Activity" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/purpleDark</item>
        <item name="colorPrimaryDark">@color/purpleDark</item>
        <item name="colorAccent">@color/orange</item>
    </style>
</resources>